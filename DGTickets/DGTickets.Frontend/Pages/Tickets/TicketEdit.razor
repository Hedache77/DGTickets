@if(ticketDTO is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <TicketForm @ref="ticketForm" TicketDTO="ticketDTO" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
